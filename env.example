# FinGuard - Variáveis de ambiente (opcional para MVP/POC)
# Copie para .env apenas se for publicar ou usar PostgreSQL local

# Backend - banco (só em prod ou docker-compose custom)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finguard
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Mobile - URL da API (só em build de produção)
# Edite src/environments/environment.prod.ts diretamente
